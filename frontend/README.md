# budget-assist frontend

This is a minimal Vite + React scaffold.

Run locally:

```bash
cd frontend
npm install
npm run dev
```

The frontend expects the backend to serve APIs at `/api/v1/*` on the same host (proxy or run both on the same origin in development).
